#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<pair<int, int> > gols(n);
    for(int i = 0; i < n; ++i){
        int x, y;
        cin >> x >> y;
        gols[i] = {x,y};
    }
    


}