#include <bits/stdc++.h>

using namespace std;

int MAX = 1000;

int main(){
  int n, m;
  cin >> n >> m;
  int matriz[n][m];

  for(int i = 0; i < n; i++)
    for(int j = 0; j < m; j++)
      cin >> matriz[i][j];
  int sx, sy, ex, ey;
  cin >> sx, sy >> ex >> ey;

  int dist[n][m];
  deque<int> q;
  q.push_back(0)



  
}